"""django_common — shared library for Django pods (skeleton, T015).

Real modules (outbox, auth middleware, RLS migration helpers, settings,
structlog config) land in later tasks; contract of record:
specs/003-platform-foundation/contracts/pod-template.md.
"""
