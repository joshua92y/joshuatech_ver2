# django-common — shared library for Django pods (skeleton, T015; real code in later tasks)
[project]
name = "django-common"
version = "0.0.0"
description = "Shared library for Django pods (skeleton)"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/django_common"]
