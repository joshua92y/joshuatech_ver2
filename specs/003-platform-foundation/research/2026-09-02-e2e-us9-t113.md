# T113 tester E2E 증거 — US9 (2026-09-02, 최종 HEAD f8fe804)

tester 보고 요약(1차 + fix 후 재판정). 환경: 브랜치 `003-platform-foundation`, pwsh 7, 전 하니스 실측.

## 1차 판정 (HEAD 8cdb3d1) — PARTIAL PASS

| 항목 | Result | Evidence |
|---|---|---|
| 기준선 run-all | PASS | `ALL PASS` — agent-layer **72/0**(rules 20·agents 18·boundary 7·미러 27), hooks 23/0, scripts 40/0, platform-harness 19/0, adr-madr 64+12/0 |
| US1 이월 서브체크 | PASS | rules 5개 `^paths:` grep → **5** (US1 E2E에서 SKIP했던 항목 해소) |
| AC1 — 경로 규칙 선택 로드 | PARTIAL | (a) 정적 행렬 15경로: 13건 의도 규칙 단독 매칭·음성 2건 무매칭. 위반 1건: `apps/web/**`이 `web.md` + `django-pod.md`(`apps/*/**`)에 동시 매칭(제외는 산문뿐). (b) 동적: `apps/identity-admin/pyproject.toml` Read 시 django-pod.md**만**, `infra/README.md` Read 시 infra.md**만** 주입 — 선택 주입 메커니즘 실동작 확인 |
| AC2 — 빌더 3종 프리로드·가드 무충돌 | PASS(정적) | frontmatter 3종 완비(TDD ×2·verification-before-completion), api-builder가 django-pod+events 바인딩. tester-write-guard는 tester.md frontmatter에만 등록 — 빌더 비적용·충돌 없음. 실디스패치는 tester 권한 밖 SKIP(컨트롤러 세션에 3종 등록 확인됨) |
| AC3 — k8s-security 경계 | PASS | SKILL.md 경계 6개 나열·템플릿 `## K8s security` 존재. 경계 파일이 AC 명명 5검사(NetworkPolicy·PSA·시크릿 경로·Access·digest) 전부 커버. **과거 실행 증거**: r2(2026-09-01):577·r3(2026-09-02):422 리뷰 파일에 실제 `## K8s security` 절 실존 |

## 컨트롤러 후속 조치

- 런타임 프로브 2건: ① 신규 규칙은 새 세션에서 정상 주입(양성 대조), ② **부정 glob(`!…`) 미지원 확정** — 제외 경로에서도 규칙 주입됨.
- fix `f8fe804`: django-pod glob을 Django 파일 형상 4개(`apps/*/pyproject.toml`·`apps/*/*.py`·`apps/*/**/*.py`·`apps/*/Dockerfile`)로 한정, Scope 문장·fastapi-pod.md 문구·kr 미러 2종 동기화.

## 재판정 (HEAD f8fe804) — AC1 PASS → US9 Overall PASS

- frontmatter 6개 glob 교체 확인, agent-layer **72/0** 재실행, run-all **ALL PASS** 재실행.
- 정적 행렬 19경로 재실행: `apps/web/README.md`·`apps/web/app/page.tsx` → **web.md 단독**(중첩 해소), Django 형상 4종 → 각자 새 glob에 정확히 매칭, 기존 단독 매칭·음성 대조군 회귀 0.

### Failures
없음(1차의 US9-AC1 항목은 f8fe804로 해소).

### Skips
- AC1 동적-web: `apps/web/` 파일 미존재(후속 task 영역)로 실주입 관측 불가 — 정적 행렬 + 타 경로 실주입 3건으로 대체.
- AC2 실디스패치: tester 권한 밖 — 정적 검증 + 가드 등록 스코프 검증으로 대체(빌더 3종은 컨트롤러 세션의 Agent 타입 목록에 실등록 확인).
- AC3 실행: `/approval-review`는 컨트롤러 스킬 — SKILL.md 정적 검증 + r2·r3 실행 산출물로 대체.

### Observations (report-only)
- 가상 `apps/web/worker.py`(.py/Dockerfile이 apps/web에 생기면) 이중 매칭 재발 — 현 설계상 apps/web은 JS 트리라 해당 형상 부재; **apps/web에 Dockerfile/.py 추가가 회귀 트리거**임을 기록.
- pod 디렉터리 내 비Django 형상(README 등)은 이제 어떤 규칙도 로드하지 않음 — Scope 문장에 명시된 의도된 축소.
- r3 리뷰 파일에 `## K8s security` 헤딩 중복(419 스텁 + 422 본문) — 외형 문제, AC 영향 없음.

### Tests written
없음 — 정적 계층은 `tests/agents/agent-layer.tests.ps1`(72 단언)이 상시 회귀로 커버. AC1 행렬 스크립트는 스크래치패드 보존(런타임 glob 의미론의 리포 내 재구현 검증은 부적절 판단).

**Overall verdict: US9 PASS** — AC1(fix 후)·AC2·AC3·기준선·US1 이월 서브체크 전부 충족.
