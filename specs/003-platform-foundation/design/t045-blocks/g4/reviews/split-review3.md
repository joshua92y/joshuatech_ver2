# T045 G4 분리 독립 task review 3

검토일: 2026-09-22. 범위: `.superpowers/sdd/tasks/g4-review3.diff`의 공통 변경 후 복구 안내와 D-16 단언만. 기존 네 findings의 Closed 판정은 유지하며 과거 범위를 다시 탐색하지 않았다.

- **Spec compliance: Approved (이번 오프라인 코드 검토 범위).** G4-R1-02의 마지막 사후 완전 단절 갈래가 닫혔다.
- **Task quality: Approved (이번 수정 범위).** 잔여 findings **0건**. 기존 검토1의 다섯 지적은 모두 Closed다.

## G4-R1-02 최종 판정: Closed

`specs/003-platform-foundation/design/t045-blocks/g4/g4-drill.ps1:301`의 공통 변경 후 안내는 이제 `$recover` 값과 무관하게 다음 두 조건을 함께 출력한다.

1. API 접근이 가능하면 읽기 전용 `g4-adopt.ps1`에서 값·UID를 판정하고 R1/R2를 따른다.
2. kubectl·SSH가 막혔으면 adopt를 실행하지 않고 **파일의 R3 노드 셸 절차를 읽어** 살아 있는 창 A 또는 OCI 비상 경로에서 복구한다.

“남은 파드는 절대 건드리지 않는다”와 “드릴을 바로 재실행하지 않는다”도 남아 있다. 따라서 변경 시도 1건 뒤 조회가 계속 실패하여 `$recover=''`인 채 끝나더라도, 실행할 수 없는 adopt 판정을 거쳐야 R3를 알 수 있던 문제가 해소됐다. 이 안내는 접근 상태를 자동 진단하거나 추가 명령을 실행하는 분기가 아니라 운영자가 따를 조건부 문면이며, 그 의미가 정확하다.

## 독립 확인

diff에 추가된 공통 안내 `if ($changes.Count -gt 0)` 조각만 추출했다. AST로 명령이 `Write-Warning` 두 개뿐임을 확인한 뒤 가짜 ArrayList 변경 로그로 로컬 실행했다. 운영 블록 전체나 finally 전체는 실행하지 않았다.

| 변경 로그 수 | API 가능 시 adopt 안내 | 실행하지 않고 파일 R3 읽기 | 드릴 즉시 재실행 금지 |
|---:|---:|---:|---:|
| 0 | 없음 | 없음 | 없음 |
| 1 | 있음 | 있음 | 있음 |

이번 수정은 기존 value/UID 안내를 변경하지 않는다. 위 변경 0 결과는 이 공통 조각만의 결과이며, 앞선 검토에서 닫힌 최초·삭제 직전 원인별 인계가 사라졌다는 뜻이 아니다.

`harness-g4.ps1:998`의 D-16은 삭제 후 kubectl 지속 실패 경로에서 기존 변경 1건·300초 종료·단일 삭제 argv 단언에 더해 `실행하지 않고 파일의 R3`를 요구한다. 제공 로그의 필요한 행만 읽어 다음을 확인했다.

- `%TEMP%/g4-review2-red.txt:174`: D-16이 해당 R3 안내 누락으로 FAIL. :200은 **121/122**, lint 실패 0.
- `%TEMP%/g4-final-green.txt:110`: D-16 PASS. :136은 **122/122**, lint 실패 0.

로그 수치는 구현자의 실행 증거를 확인한 것이며 독립 전체 하네스 재실행 결과로 표현하지 않는다.

## 범위와 한계

이번 diff 밖의 본체·하네스는 다시 검토하지 않았다. 운영 자격 접근, 실제 kubectl·ssh·oci·Vault 호출, 원본 블록 전체 실행, 전체 하네스/변이 중복 실행, 커밋, 하위 에이전트 생성은 없었다. 작업트리 소스 수정 없이 이 보고서만 작성했다.

Approved는 이번 오프라인 코드 검토의 합격이다. 구현자가 진행 중인 변이 223개 전수, 후속 문서·인계 및 라이브 준비 완료까지 합격했다고 뜻하지 않는다. 변이 전수는 이번 검토 범위 밖이다.
