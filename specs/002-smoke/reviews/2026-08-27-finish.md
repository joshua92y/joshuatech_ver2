# Finish review — 002-smoke (2026-08-27)
Status: Approved

리뷰어 4명(general-purpose 서브에이전트, 병렬)에게 경계 rubric 전문과 명시된 입력만 전달. 전원 ✅. 비차단 지적은 이 커밋에 반영했다(아래 각 절 끝).

## Report vs diff
Verdict: ✅
- 기준 `git merge-base 001-claude-setup HEAD` = 859b321 → 17 files / +1651 / −4, 커밋 36개 — report §Changes Made와 일치(numstat 17개 파일 전부 일치). `main` 기준(32c631c)은 001 전체(133 files)를 포함해 오도되므로 001 기준을 명시한 것이 옳음.
- 검증 주장 재현: 하네스 `40 passed, 0 failed`(runs=21), run-all 12건 PASS → `ALL PASS`, 실물 `(unchanged)`, 미체크 task 0, TDD RED 증적(T001 0/1 · T003 1/6 · T007 10/3 · T010 19/19 · T017 39/1)을 각 테스트 커밋 트리에서 재실행해 확인, tester 수치(US1 4·US2 3·US3 5·Edge 10 PASS) 일치, 미실행 항목 명시.
- 커밋 36개 모두 Conventional Commits + `Co-Authored-By` 트레일러.
- Next: Phase 7 T017–T019 전부 `[X]`, 이월 항목·수용 리스크·archive 후속 기재.
- 반영한 nit: spec 줄의 "Edge 17" → "Edge Cases 15"; 결함 3건 귀속을 "리뷰·converge 루프(셋째는 converge 1차 F1)"로 정정; "(이 커밋에서 추가)" 목록에 tasks.md T019 `[X]` 추가; converge 2차 수치는 대화 기록 외 검증 불가임을 인지(report에 그대로 둠).

## E2E evidence
Verdict: ✅

| Story | Result | Evidence ok? |
|---|---|---|
| US1 인덱스 재생성 (AC1–4) | PASS | ✅ 리뷰어 직접 재현: 실저장소 `(unchanged)`·트리 클린·두 행이 헤더와 일치; AC2/AC4는 하네스 US1-2/Edge-6/US1-4, AC3 멱등 2회차 확인 |
| US2 Status 정규화 (AC1–3) | PASS | ✅ 픽스처 재현 3건 + 하네스 US2-1~7(앵커 케이스 포함) |
| US3 결측·예외 (AC1–5) | PASS | ✅ 재현: `020-empty` 경고+exit 0, Status 없음 → `error: … missing **Status** line`·exit 1·README 미생성·tmp 0, Priority 없음 `—`, plan 없음 spec만; AC2·H1 누락은 하네스 US3-2/Edge-8/US3-6 |
| Edge Cases | PASS | ✅ 재현: 표 헤더 2개 오류·불변, `specs/` 없음 오류; 나머지는 하네스 Edge-1~17 |

- 테스트 파일 추가 없음(기존 하네스 40 단언이 동일 계약) — 수용. `tests/scripts/fixtures/` 없음, 트리 클린.
- 반영한 gap: E2E 보고서 원문(스토리별 표)이 저장소에 없었음 → report.md 끝에 `### tester E2E 보고서 원문(2026-08-27)`으로 수록. 보고서 HEAD bc299e1과 현재 HEAD의 차이는 tasks.md 체크뿐(코드 무변화).
- 비차단: 동시 2회 실행·2000자 제목은 tester만 실행(자동 단언 없음, spec Edge 목록 밖).

## Study contract
Verdict: ✅
- frontmatter 필수 7키 유효, `pubDate` 2026-08-27, `draft: true`, `change: "002-smoke"`, `sources` 8건(경로 6건 실재, URL 2건 https), series `sp-0-claude-setup` / seriesOrder 2.
- 다섯 절 정확히 1회씩 순서대로, 전부 비어 있지 않음; 개념·기각 대안(표 9행)·검증(40/0, 12 PASS, 22 PASS, RED→GREEN)이 세션 밖 독자 기준으로 서술됨; 수치가 report와 일치.
- 파일명 ASCII, UTF-8 BOM 없음, LF, 비밀·개인정보 없음; MDX 안전(코드 스팬 밖 `<>{}` 없음, 표 셀 파이프 이스케이프).
- 반영한 nit: 30행 코드 스팬 안의 실제 U+FEFF 문자를 눈에 보이는 `` "`u{FEFF}abc" `` 표기로 교체; 운영 교훈(Bash heredoc·백슬래시)을 report.md Validation에도 기록해 sources와 정합.

## Decisions
Verdict: ✅
- ADR 불필요(확인): 프레임워크·경계·데이터 소유·프로토콜·컨벤션 신설 없음. "인덱스는 헤더에서 재생성"은 이 feature 이전의 `.claude/rules/specs.md`·001 FR-010이 정한 규칙이고, "표 = 스크립트 소유, 머리말·후행 = 사람 소유"는 그 규칙의 feature 국소 구체화로 spec Key Entities·research R2에 기록됨(ADR-0000의 "횡단 결정만 ADR" 경계에 부합). 같은 패턴이 다른 파생물로 확장되는 시점에 일반화 ADR 검토(이월).
- 헌법 개정 없음: `git diff 859b321...HEAD -- .specify/memory/constitution.md` 0바이트, 1.0.0 유지. plan Complexity Tracking "해당 없음".
- converge unrequested 2건(T019)은 report `### 계약 외 동작`에 정당화, T017은 research R7 추기로 사유 기록.
- ADR-0000/0001과 모순 없음(스크립트는 저장소 소유 도구, `specs/NNN-slug/` 읽기 전용, `.specify/` 변경 0).
- 비차단 관찰(report Next로 이월): AGENTS.md Layout에 `scripts/` 추가; "pwsh 7.6.5 이상" 하한을 저장소 전체 전제로 삼으려면 CLAUDE.md Prerequisites에 기재.

## Issues
(없음)
