# Approval review — 002-smoke (2026-08-27)
Inputs: spec.md (Status: Draft), plan.md, tasks.md, checklists: 28 unchecked (requirements-quality.md; 내장 requirements.md 0), /speckit-analyze: 6 findings (CRITICAL 0 · HIGH 0 · MEDIUM 1 · LOW 5; I1·I2·A1은 리뷰 전 정정 c095380)

리뷰어 5명(general-purpose 서브에이전트, 병렬)에게 경계 rubric 전문 + 발췌(spec: User Scenarios & Testing·Requirements·Key Entities / plan: Summary·Technical Context·Constitution Check·Project Structure·Complexity Tracking / tasks: phase 제목·task 줄 / 헌법 Core Principles)만 전달. trends만 WebSearch 허용.

## Security
| 항목 | 상태 | 비고 |
|---|---|---|
| 인증·인가 (who may call what; default deny; admin surfaces) | ⚠️ | 로컬 CLI라 인증 표면 자체는 없음(호출자 = 저장소 쓰기 권한자, 관리자 표면 없음). 다만 tasks T006(a)·T009가 `tester`에게 실저장소 `specs/README.md`(테스트 경로 아님)를 스크립트로 다시 쓰게 하고, T012는 `$env:TEMP`로 tester-write-guard를 우회하도록 안내 → 이 저장소의 쓰기 경계 의도와 어긋남(F7, low) |
| 입력 검증·인젝션 | ⚠️ | 디렉터리명 허용 목록 정규식으로 링크·메시지에 들어가는 값이 통제되고, 파싱 정규식은 줄 단위·앵커 고정·선형이라 ReDoS 없음. 명령 실행·템플릿·업로드 없음. 남은 문제: `\|` 이중 이스케이프로 표 파손(F2), `\d{3,}` 무제한 + `[long]` 캐스트 오버플로(F3), 헤더 값의 마크다운 링크/이미지가 README에 그대로 전파(F4), `-Root`·`$PSScriptRoot`·reparse point 처리 미정의(F5) |
| 시크릿 | ✅ | 시크릿·설정 파일·환경 변수 사용 없음. 로그는 건수·상대 디렉터리명·계약된 메시지만 출력 |
| 데이터 노출 | ⚠️ | 제품 데이터·PII 없음. 그러나 `error: specs directory not found: <절대 경로>`와 미처리 .NET 예외가 절대 경로·스택을 stderr에 내고, T016이 실제 출력을 quickstart에, tester가 증거를 보고서에 커밋 → 계정명·로컬 경로 유입 가능(F1, low) |
| 전송·저장 | ⚠️ | 네트워크 없음, 민감 필드 없음. `WriteAllText`가 원자적이지 않아 중단 시 README가 잘린 채 남고, README가 심볼릭 링크면 대상 경로에 씀(F6, low) |
| 공급망 | ✅ | 신규 의존성 0(.NET 내장 API만), pwsh 7은 이미 필수 도구. 임시 픽스처명 `specidx-<guid>` 예측 불가 |
| 남용 | — | 네트워크·서비스·세션 없음 |
### Findings
- **F1 low** — 오류/예외 출력의 절대 경로가 커밋 산출물로 유입. 수정: I/O를 try/catch로 감싸 계약된 한 줄만 stderr, 스택 금지; 기록 시 경로 치환.
- **F2 low** — `|` 이스케이프가 이미 이스케이프된 `\|`를 깨뜨림(FR-013). 수정: `\`→`\\` 먼저, 그다음 `|`→`\|`; 테스트 케이스 추가.
- **F3 low** — `\d{3,}` 무제한(유니코드 숫자 포함) + `[long]` 캐스트 → 미처리 예외. 수정: `[0-9]`로 한정, 캐스트 없는 (길이, ordinal) 정렬.
- **F4 low** — spec 헤더의 마크다운/HTML이 인덱스에 그대로 전파. 잔여 위험 낮음(신뢰된 커미터, spec.md도 렌더링됨).
- **F5 low** — `-Root`·`$PSScriptRoot`·reparse point 처리 미정의. 수정: `GetFullPath` 정규화, `$PSScriptRoot` 비면 오류, `-Attributes !ReparsePoint`, `Test-Path -PathType Leaf`.
- **F6 low** — README 쓰기가 원자적이지 않음. 수정: 임시 파일 + `File.Move(..., $true)`.
- **F7 low** — tester가 테스트 경로 밖(실저장소 README)을 쓰도록 지시됨. 수정: E2E를 T013 이후로, 기대 `(unchanged)` + `git diff --exit-code`; 픽스처는 `tests/scripts/fixtures/<guid>/`.

## Tenant & data boundary
| 항목 | 상태 | 비고 |
|---|---|---|
| Key Entity마다 소유자·격리 키(또는 전역인 이유) 명시 | ⚠️ | "Feature 인덱스 항목"은 소유자·전역 이유·원천→파생 관계를 모두 명시해 헌법 III 문구를 충족. "인덱스 문서"(`specs/README.md`)는 구조만 있고 소유자·전역 문장이 없음 — 표(스크립트 소유)와 머리말·후행(사람 소유)의 소유 분리가 암시로만 존재(Finding 1) |
| 서비스 간 테이블 공유·암묵적 조인 없음 | ✅ | 서비스 경계 자체가 없음. 읽기 계약 = spec.md 헤더 형식, 출력 계약 = FR-006 표 스키마와 contracts/cli.md. 계약이 깨지면 FR-009로 fail-closed |
| 마이그레이션 | — | DB·스키마 마이그레이션 없음. 일회성 전환(T013·T015)은 커밋 단위로 revert 가능 |
| 데이터 수명주기 | ⚠️ | 재생성·오류 시 미변경·롤백은 명시. 입력 읽기 전용, 백업·이력 = git, 손편집 표 셀은 다음 실행에서 폐기됨은 문장으로 고정돼 있지 않음(Finding 2) |
| 단일 테넌트 하드코딩 없음, 격리 테스트 | ✅ | 기본 Root는 스크립트가 속한 저장소이며 `-Root`로 대체 가능. 임시 픽스처(`specidx-<guid>`)·독립 Root·오류 경로 README 불변 단언. "픽스처 실행 중 실제 저장소 README 불변" 부정 단언만 없음(Finding 2(b)) |
### Findings
- **Finding 1 low** — "인덱스 문서" 엔티티의 소유자·전역 문장 누락. 수정: Key Entities에 소유 분리 문장 추가.
- **Finding 2 low** — 입력 읽기 전용·백업/삭제 범위가 명시 문장이 아니라 유추. 수정: plan Storage 줄 보강; 테스트에 입력 불변·실저장소 README 불변 단언 추가.

## Operability
| 항목 | 상태 | 비고 |
|---|---|---|
| 로그 구조화·상관 id·비밀 없음·건강 지표 | ⚠️ | stdout 1줄 요약 + stderr `warning:`/`error:` 고정 문법 — 단일 CLI에 맞는 구조화. 상관 id 생략 사유(단일 프로세스, 단일 출력 파일) 수용 가능. 건강 지표(run-all `scripts` + 검사 8)는 "누락 행"만 잡고 "낡은 셀"(Status 변경 후 미재생성)을 잡지 못함(Finding 1) |
| 롤백 경로 | ✅ | `git checkout -- specs/README.md`, 스크립트 제거 = 커밋 revert, 데이터 이전 없음 |
| 실패 모드·타임아웃·재시도·멱등성 | ⚠️ | 헤더 결손 fail-closed·`specs/` 부재·경고+exit 0·멱등(LastWriteTime 불변까지)·BOM/CRLF 정규화는 양호. 빠진 것: (a) 쓰기 권한 거부·잠금·읽기 실패 시 .NET 예외가 그대로 터져 `error:` 형식과 exit 1 약속이 검증되지 않음(Finding 2); (b) 비원자적 쓰기·동시 실행 미기술(Finding 3); (c) UTF-8이 아니거나 깨진 바이트가 U+FFFD로 조용히 통과(Finding 4). 타임아웃·재시도 해당 없음 |
| 런북 필요성 | ⚠️ | 계획에 판단 자체가 없음. 별도 런북은 과함(헌법 V)이나 운영 안내의 영구 위치가 필요(Finding 5) |
| 비용·한계 | ✅ | 로컬 실행, 쿼터 없음. SC-005 5초 단언은 여유 충분 |
### Findings
- **1 medium** — 명명된 건강 지표가 낡은 인덱스를 감지하지 못함. 수정: run-all에 `specs-index-fresh` 검사(스크립트 실행 → `(unchanged)` 기대; 부작용 주석) 추가.
- **2 medium** — I/O 실패의 메시지·종료 코드가 계약에 없고 테스트되지 않음. 수정: 본문 try/catch → `error: <message>` + exit 1; 읽기 전용 README 테스트; 계약에 한 줄 추가.
- **3 low** — 비원자적 쓰기·동시 실행 미기술. 수정: 임시 파일 + Move; plan 실패 모드에 명시.
- **4 low** — 잘못된 UTF-8 입력이 조용히 통과. 수정: `UTF8Encoding($false, $true)`로 엄격 디코딩 + 테스트.
- **5 low** — 런북 필요성 미판단. 수정: plan에 "런북 불필요, 복구 절차는 스크립트 헤더 주석 + AGENTS.md Commands 행" 명시.

## Trends
| 기술 | 현재 plan | 최신 동향 | 제안 | 출처 |
|---|---|---|---|---|
| PowerShell 7.6 (`pwsh`) | "PowerShell 7.6"; 개발기 7.6.5 | 7.6은 LTS(2026-03-18 GA, .NET 10, EOS 2028-11-14). 현재 LTS 패치 7.6.5, Stable 7.5.10. 7.4 LTS·7.5는 2026-11-10 EOS. tasks가 쓰는 cmdlet·.NET API에 deprecation 없음. `pwsh -File` 종료 코드 규칙은 하네스 캡처와 일치 | "7.6 LTS, 7.6.5 이상"으로 패치 하한 명시(Findings 1) | https://learn.microsoft.com/en-us/powershell/scripting/install/powershell-support-lifecycle · https://learn.microsoft.com/en-us/powershell/scripting/whats-new/what-s-new-in-powershell-76 · https://learn.microsoft.com/en-us/powershell/module/microsoft.powershell.core/about/about_pwsh |
| 테스트 하네스(프레임워크 없음) vs Pester / PSScriptAnalyzer | 자체 Assert 하네스, exit 0/1 | Pester 6.1.0(2026-08-11), 5.9.1 유지선; pwsh 7에 미동봉(PSGallery 설치 필요). PSScriptAnalyzer 1.25.0(2026-03-20), 미동봉. PSSA `PSUseApprovedVerbs`·`PSUseSingularNouns`가 `Normalize-Status`·`Remove-Fixtures`를 경고 | "새 의존성 0" 판단 타당. 함수명만 승인 동사·단수 명사로(Findings 2) | https://www.powershellgallery.com/packages/Pester · https://pester.dev/docs/introduction/installation · https://www.powershellgallery.com/packages/PSScriptAnalyzer/1.25.0 · https://learn.microsoft.com/en-us/powershell/scripting/developer/cmdlet/approved-verbs-for-windows-powershell-commands |
| 정규식 헤더 파싱 vs Markdig | 줄 단위 정규식, `Replace(s,r,1)` | Markdig 1.3.2(2026-06-18)는 NuGet → 새 의존성. GFM ATX 제목 규칙과 `^#\s+` 일치. 정규식은 선형에 가까움(중첩 수량자 없음); 입력이 저장소 내 신뢰 파일 | 유지 | https://www.nuget.org/packages/Markdig · https://github.github.com/gfm/ · https://learn.microsoft.com/en-us/dotnet/standard/base-types/best-practices-regex · https://learn.microsoft.com/en-us/dotnet/api/system.text.regularexpressions.regex.replace |
| 파일 쓰기: .NET API vs `Set-Content` | `WriteAllText` + `UTF8Encoding($false)`, LF 결합 | pwsh 7 기본 인코딩은 utf8NoBOM이지만 줄 끝은 `Environment.NewLine`(Windows CRLF)이고 선택 매개변수 없음(PowerShell #16569). `File.WriteAllText(string,string)`도 BOM 없는 UTF-8 기본; `ReadAllText`는 BOM 자동 소비 | 유지(권장 우회책과 일치) | https://learn.microsoft.com/en-us/powershell/module/microsoft.powershell.core/about/about_character_encoding · https://github.com/PowerShell/PowerShell/discussions/16569 · https://learn.microsoft.com/en-us/dotnet/api/system.io.file.writealltext |
| 멱등 생성 블록: 마커 없는 표 감지 vs `<!-- START/END -->` | `^\| # \|` 첫 줄부터 연속 `^\|` 블록, 비교 후 기록 | doctoc·markdown-toc·terraform-docs·cog는 모두 명시적 마커; terraform-docs는 마커 없으면 append(plan과 같은 전략). plan 방식은 README가 스크립트 소유 파일이고 헤더가 고정 시그니처라 실용적으로 충분 | 유지. 선택: `^\| # \|` 줄이 2개 이상이면 fail-closed | https://github.com/thlorenz/doctoc · https://github.com/jonschlinkert/markdown-toc · https://terraform-docs.io/user-guide/configuration/output/ · https://cog.readthedocs.io/en/latest/ |
| 크로스플랫폼 경로 처리 | `Join-Path`/.NET, `$PSScriptRoot`, 비재귀 열거 | `Join-Path`는 7.6부터 `-ChildPath string[]`(단일 문자열 호환); Linux/macOS는 파일명 대소문자 구분(소문자 고정 규칙이 흡수); `Get-ChildItem`은 링크를 포함하되 `-Recurse` 없이는 따라가지 않음; `-ExecutionPolicy`는 비Windows에서 무시 | 유지 | https://learn.microsoft.com/en-us/powershell/module/microsoft.powershell.management/join-path · https://learn.microsoft.com/en-us/powershell/scripting/whats-new/unix-support · https://learn.microsoft.com/en-us/powershell/module/microsoft.powershell.management/get-childitem |
| stderr 출력 방식 | `[Console]::Error.WriteLine` + `exit 1` | `Write-Error`는 ErrorRecord를 여러 줄로 렌더링 → 줄 단위 정확 비교·native 소비에는 평문 stderr가 맞음 | 유지 | https://learn.microsoft.com/en-us/powershell/module/microsoft.powershell.core/about/about_redirection |
| 보안 권고(2025–2026) | 언급 없음 | 2026-08-17 PowerShell 권고 3건: CVE-2026-50523(로컬 command injection, Windows), CVE-2026-70337(web cmdlet traversal RCE), CVE-2026-70338(code injection) — 7.6.5/7.5.10/7.4.19에서 수정. 2026-04-14 CVE-2026-26143은 `Import-PowerShellDataFile -SkipLimitCheck`만 해당. 스크립트는 web cmdlet 미사용, 개발기 7.6.5는 패치본 | "7.6.5 이상" 명시(Findings 1) | https://github.com/PowerShell/Announcements/issues/86 · https://github.com/PowerShell/Announcements/issues/87 · https://github.com/PowerShell/Announcements/issues/88 |
### Findings
- **1 낮음** — plan Technical Context "PowerShell 7.6"에 패치 하한 `7.6.5 이상` 명시, quickstart 전제조건 반영. https://github.com/PowerShell/Announcements/issues/86 , https://learn.microsoft.com/en-us/powershell/scripting/install/powershell-support-lifecycle
- **2 낮음** — `Normalize-Status` → 승인 동사(`ConvertTo-NormalizedStatus`), `Remove-Fixtures` → 단수(`Remove-Fixture`). https://learn.microsoft.com/en-us/powershell/utility-modules/psscriptanalyzer/rules/useapprovedverbs
- 그 외(정규식 파싱, .NET 파일 API, 마커 없는 표 감지, 경로 처리, 하네스 선택)는 현재 권장 관행과 일치 — 변경 제안 없음.

## Spec consistency
| 항목 | 상태 | 비고 |
|---|---|---|
| 모든 FR·User Story가 task ≥1개로 커버, 모든 task 역추적 | ✅ | 독자 매핑: FR-001~015·US1-1~4·US2-1~3·US3-1~5·SC-001~007 전부 task에 닿음(analyze와 일치). T015는 plan §Project Structure로만 추적 — 문서 정리 task라 허용. Edge Cases 2건(중복 Status/Priority 줄, README BOM/CRLF)은 테스트 단언이 없었음(F1·F2) |
| `[NEEDS CLARIFICATION]` 없음; spec–plan 모순 없음 | ⚠️ | 마커 0. spec↔plan 모순 없음. 잔존 spec↔tasks 불일치 1건: ordinal 정렬이 tasks에 미반영(F3, LOW). plan 문서 트리에 requirements-quality.md 누락(F9) |
| plan Constitution Check 통과 | ✅ | I~VI PASS, Complexity Tracking 해당 없음. 근거와 일치 |
| 스토리 phase마다 테스트 선행 + E2E 1개 | ✅ | US1: T003→T004·T005→T006, US2: T007→T008→T009, US3: T010→T011→T012. 회색 지대: T002가 `specs/` 부재 오류 경로를 실패 테스트 없이 먼저 작성(F4, LOW). E2E를 T013 이후 일괄 실행은 헌법 II 위반 아님(F5) |
| analyze triage | ✅ | CRITICAL 0·HIGH 0. I1 삼자(US3-4↔FR-010↔T010(3)) 일치 확인, I2 반영 확인, A1은 tasks 미반영(F3) |
| 미체크 체크리스트 판정 | ⚠️ | requirements.md 0 미체크; requirements-quality.md 28/28 미체크 — 리뷰어 소유, Notes에 해소 기록 있음 → **차단 아님**. 근거 있는 항목은 체크하고 근거 파일을 적을 것(F10) |
### Findings
- analyze CRITICAL/HIGH: 없음. 미커버 FR/US/SC: 없음.
- **F1 MEDIUM(비차단)** — README 입력의 BOM/CRLF 정규화가 테스트되지 않고 "변경 없음" 비교 기준이 모호(정규화 후 텍스트와 비교하면 CRLF README가 영원히 `(unchanged)`). 수정: 디스크 원문과 비교, 테스트 추가.
- **F2 LOW** — 중복 Status/Priority 줄 픽스처 없음. 수정: T010(12).
- **F3 LOW** — ordinal 정렬이 T004·T010(7)에 미반영. 수정: `[StringComparer]::Ordinal`, `040-a-b` vs `040-ab` 픽스처.
- **F4 LOW** — T002가 실패 테스트 없이 오류 경로를 구현. 수정: T001에 단언 1개(RED) → T002 GREEN.
- **F5 ✅ 허용** — E2E(T006/T009/T012)를 T013 이후 일괄 실행: 헌법 II·CLAUDE.md lifecycle 6과 정합, tester가 저장소 파일을 바꾸지 않아 오히려 안전. 명문화 권고.
- **F6 ✅ 허용** — T003(4) 스크립트 복사 방식 유효. nit: `Pop-Location` in finally, `$env:TEMP\specs` 미생성 단언.
- **F7 LOW** — T009 전제가 실물 상태에 고정. 수정: 실행 시점 값 X 기록 + 규칙 검증, 주석 없으면 픽스처 대체.
- **F8 LOW** — T013에 "표 블록 1개, 기존 표 교체됨" 확인 추가.
- **F9 nit** — plan 문서 트리·Constitution Check I에 requirements-quality.md 반영.
- **F10 비차단** — 근거 있는 체크리스트 항목 체크 + 근거 위치 기록.
- 종합: 승인 차단 항목 없음.

## 종합 의견
**판정**: 승인 권고
- 근거: CRITICAL/HIGH 0, 헌법 위반 0, 요구사항 커버리지 100%(FR 15 + SC 7 → task 16). 경계 5곳의 지적(MEDIUM 4 · LOW 15 · nit 2)은 전부 spec/plan/tasks/계약 수준의 보강이며, 승인 후에는 이 문서들이 읽기 전용이 되므로 **승인 전에 모두 반영했다**(아래 표). 남은 것은 명시적으로 수용한 리스크 2건뿐이다.
- 수정 필요 항목: 없음(반영 완료).

**리뷰 반영 (승인 전, 같은 커밋)**

| 지적 | 반영 위치 |
|---|---|
| Sec F1 / Ops 2 — I/O 예외가 스택·절대 경로로 노출, 계약 미검증 | tasks T011 try/catch → `error: <message>` + exit 1; T010(14) 읽기 전용 README 단언; contracts/cli.md 오류 줄; spec Edge Cases(읽기·쓰기 실패); tasks Path Conventions·T006/T012/T016 경로 `<REPO>`/`<TEMP>` 치환 |
| Sec F2 — `\|` 이중 이스케이프 | tasks T005(`\`→`\\` 선행), T010(8) `A \| B` 케이스; plan §3 |
| Sec F3 — `\d{3,}` 유니코드·`[long]` 오버플로 | tasks T004 `[0-9]{3,}` + 캐스트 없는 (길이, ordinal) 정렬; plan §1·§4; contracts/cli.md |
| Sec F5 — 경로·reparse point | tasks T002 `GetFullPath`·`$PSScriptRoot` 비면 오류; T004 `-Attributes !ReparsePoint`·`-PathType Leaf`; contracts/cli.md `cannot resolve repository root` |
| Sec F6 / Ops 3 — 비원자적 쓰기 | tasks T005 임시 파일 + `File.Move(..., $true)`; plan §6·§Observability 실패 모드; contracts/cli.md |
| Sec F7 / SpecCon F5 — tester 쓰기 경계 | tasks T006(a) `(unchanged)` + `git diff --exit-code`, T006(d)/T009/T012 픽스처를 `tests/scripts/fixtures/<guid>/`; Dependencies에 "E2E는 T013 이후 tester 1회 dispatch" 명문화; plan Project Structure |
| Tenant 1 — "인덱스 문서" 소유자 | spec Key Entities 소유 분리 문장 |
| Tenant 2 — 입력 읽기 전용·백업=git·격리 단언 | plan Storage; spec Key Entities; tasks T003(6) 입력 불변·실저장소 README 불변 단언 |
| Ops 1 — 낡은 인덱스 미감지 | tasks T014 `specs-index-fresh` 검사; plan §8·§Observability 건강 지표; quickstart 6 |
| Ops 4 — 잘못된 UTF-8 조용히 통과 | tasks T004 엄격 디코딩, T010(13); plan §1·Constraints |
| Ops 5 — 런북 판단 | plan §Observability "런북 불필요 + 복구 절차 위치"; tasks T002 헤더 주석 복구 절차 |
| Trends 1 — pwsh 7.6.5 하한 | plan Technical Context; quickstart 전제; tasks Path Conventions |
| Trends 2 — 승인 동사·단수 명사 | tasks T001 `Remove-Fixture`, T008 `ConvertTo-NormalizedStatus`, Path Conventions |
| Trends(선택) / SpecCon F8 — 표 헤더 중복 | spec Edge Cases; tasks T005 `multiple index tables` 오류, T010(16), T013 표 1개 확인; contracts/cli.md; plan §5 |
| SpecCon F1 — README BOM/CRLF 비교 기준 | tasks T005 "디스크 원문과 비교", T010(15); plan §6 |
| SpecCon F2 — 중복 Status/Priority 줄 테스트 | tasks T010(12) |
| SpecCon F3 — ordinal 정렬 tasks 미반영 | tasks T004 `[StringComparer]::Ordinal`, T010(7) `040-a-b` vs `040-ab` |
| SpecCon F4 — T002 테스트 선행 | tasks T001 단언 1개(RED) → T002 GREEN; plan Constitution Check II |
| SpecCon F6 nit | tasks T003(4) `Pop-Location`·`$env:TEMP/specs` 미생성 단언 |
| SpecCon F7 — T009 전제 고정 | tasks T009 완화 |
| SpecCon F9 nit | plan 문서 트리·Constitution Check I |
| SpecCon F10 — 체크리스트 | checklists/requirements-quality.md 근거 있는 11개 `[x]` + 근거 기록(17개는 승인자 판단 몫) |

**수용한 리스크(변경 없음)**
1. Sec F4 — spec 헤더의 마크다운/HTML이 인덱스에 그대로 전파: 작성자는 신뢰된 커미터이고 spec.md 자체도 렌더링됨(plan §Observability에 기록).
2. Sec F5(b) — `specs/README.md`가 심볼릭 링크인 경우 미지원(Windows git 기본 설정에서는 파일로 체크아웃됨).

## 사용자 결정
- [x] 승인 (2026-08-27) — 사용자 joshua, AskUserQuestion 응답 "승인"
